export type PaymentProviderSandboxFlags = {
  stripe: boolean;
  paypal: boolean;
  wise: boolean;
};

export type PaymentProviderLiveFlags = {
  stripe: boolean;
  paypal: boolean;
  wise: boolean;
};

